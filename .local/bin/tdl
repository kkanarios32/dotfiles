#!/usr/bin/env bash

function dmenuopt () {
    echo "-i -c -h 25" 
}

filter=$(echo "" | dmenu $(dmenuopt) -p "Enter filter:")

[[ -z $filter ]] && exit 1

cmd="todoist --color list -f ${filter} | less -R"

alacritty --class Show -e zsh -c "$cmd"
