#!/usr/bin/bash

_have() { type "$1" &>/dev/null; }
! _have mods && echo "requires charmbracelet/mods" && exit 1

TMPFILE=$(mktemp)
kitty --class Show nvim "${TMPFILE}"
PROMPT=$(cat ${TMPFILE})

mods "'${PROMPT}'"
rm $TMPFILE
