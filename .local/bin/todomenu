#!/usr/bin/env bash

function dmenuopt () { echo "-i -c -l 10 -h 25"
}	#Dmenu options, so as not to clutter the rest of the script. These are the options that I use, change them if you'd like

sel=$(echo -e "Add todo:" | dmenu $(dmenuopt))
echo $sel
todoist add "$sel"
